import styled from 'styled-components';
import appTheme from '../theme/theme';

export default styled.section`
  padding: ${appTheme.spacing.unit * 9}px ${appTheme.spacing.unit * 3.5}px
    ${appTheme.spacing.unit * 3}px;
`;
